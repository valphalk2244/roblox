import type { NextConfig } from "next"

const nextConfig: NextConfig = {
  /* config options here */
}

export default nextConfig
module.exports = {
  images: {
    domains: ["https://www.alleycat.org"], // ใส่โดเมนของรูปที่ใช้
  },
}
